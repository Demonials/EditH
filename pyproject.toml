[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "edith-bot"
version = "1.0.0"
dependencies = [
    "discord.py==2.3.2",
    "python-dotenv==1.0.0",
    "requests==2.31.0",
    "aiohttp==3.9.1"
]
